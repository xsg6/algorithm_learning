ros2操作系统
## DDS（Data Distribution Service，数据分发服务）-去中心化，基于话题（Topic）的异步数据分发
## RTPS（Real-Time Publish-Subscribe Protocol，实时发布订阅协议）
## 发布、订阅、话题
1. 发布（类）：
2. 话题：唯一标识话题名 
``` c++
// 1. 发布者创建时指定话题名
pub_ = this->create_publisher<std_msgs::msg::String>("chatter", 10);
//                                                   👆 话题名  👆 QoS（缓存最后10条）

// 2. 订阅者创建时指定相同的话题名（才能匹配）
sub_ = this->create_subscription<std_msgs::msg::String>(
    "chatter",  // 👆 和发布者完全一致的话题名
    10,
    std::bind(&SubscriberNode::sub_callback, this, std::placeholders::_1)
);
```
## 节点间的依赖关系管理
主要通过package.xml中的depend字段
## 一些命令
1. ros2 run
2. source install/setup.bash 刷新环境
3. # 同样先加载Jazzy环境（每个新终端都要执行）
source /opt/ros/jazzy/setup.bash
## 海龟模拟器的命令参考
eg： ros2 node info /turtlesim
/turtlesim
  Subscribers:
    /parameter_events: rcl_interfaces/msg/ParameterEvent
    /turtle1/cmd_vel: geometry_msgs/msg/Twist
  Publishers:
    /parameter_events: rcl_interfaces/msg/ParameterEvent
    /rosout: rcl_interfaces/msg/Log
    /turtle1/color_sensor: turtlesim/msg/Color
    /turtle1/pose: turtlesim/msg/Pose
  Service Servers:
    /clear: std_srvs/srv/Empty
    /kill: turtlesim/srv/Kill
    /reset: std_srvs/srv/Empty
    /spawn: turtlesim/srv/Spawn
前半部分是话题，后半部分是信息类型
/parameter_events：ROS 2 的 “参数变化广播频道”
1. 先确保turtlesim_node在运行，然后查看所有话题
ros2 topic list 输出里会包含 /turtle1/pose
2. 实时查看/turtle1/pose话题的内容（会持续打印数据）
ros2 topic echo /turtle1/pose
3. 查看消息接口信息类型的定义
ros2 interface show turtlesim/srv/Kill(后面是一个信息类型即可)
4. ros2 topic info
ROS 2 的话题信息查询命令，核心作用是：快速获取指定话题的关键元信息，包括「消息类型、发布者数量 / 名称、订阅者数量 / 名称