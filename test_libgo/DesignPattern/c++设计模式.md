c++不同的设计模式
## 单例模式
单例模式是最常用的设计模式之一，核心目标是保证一个类在程序生命周期内只有一个实例，并提供全局唯一的访问入口
1. 单例模式的核心要求
    1. 唯一性：类的实例只能有一个，禁止外部通过 new 或拷贝创建多个实例；
    2. 全局访问：提供一个公共的静态方法（如 getInstance()），让外部能获取这个唯一实例；
    3. 懒加载 / 饿加载：可选择 “用到时再创建”（懒汉）或 “程序启动就创建”（饿汉）。